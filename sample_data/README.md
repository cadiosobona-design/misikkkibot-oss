# Sample Market Data

`btc_usdt_1m.csv` is synthetic OHLCV data for the paper-trading demo. It is not market advice and is not intended to model realistic performance.

The data is deliberately small so tests and first-run demos can execute without network access or credentials.
